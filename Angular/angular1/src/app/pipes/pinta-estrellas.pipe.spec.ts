import { PintaEstrellasPipe } from './pinta-estrellas.pipe';

describe('PintaEstrellasPipe', () => {
  it('create an instance', () => {
    const pipe = new PintaEstrellasPipe();
    expect(pipe).toBeTruthy();
  });
});
